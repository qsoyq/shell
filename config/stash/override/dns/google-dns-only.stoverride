name: google-dns-only
desc: 使用 google 进行域名解析
category: DNS
icon: https://cdn.jsdelivr.net/gh/Koolson/Qure/IconSet/Color/Google.png

dns:
  default-nameserver: #!replace
    - 8.8.8.8

  nameserver: #!replace
    # Google DNS
    - https://dns.google/dns-query
